{
  "name": "hotseatwebapp-server",
  "version": "1.0.0",
  "description": "Server for the Hot Seat Web App",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "rm -rf public && cd ../HotSeatWebApp-Client && ng build && cd dist/hot-seat-web-app-client && cp -r /server ../HotSeatWebApp-Server/public"
  },
  "author": "Zachary Wilson",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
